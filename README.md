# Vite React Starter

A modern React starter template with Vite, TypeScript, Tailwind CSS, and more.

## Getting Started

1. Clone this repository:
   ```bash
   git clone https://github.com/basvandriel/vite-react-starter.git my-project
   cd my-project
   ```

2. Install dependencies:
   ```bash
   npm install
   ```

3. Run the setup script to customize your project:
   ```bash
   node bin/init.js
   ```
   This will prompt for your project name and update `package.json` accordingly. Remove the script afterwards.

4. Start developing:
   ```bash
   npm run dev
   ```

## Scripts

- `npm run dev` - Start the development server
- `npm run build` - Build for production
- `npm run preview` - Preview the production build
- `npm run lint` - Run ESLint
- `npm run test` - Run tests
- `npm run test:ui` - Run tests with UI
- `npm run test:coverage` - Run tests with coverage

## Features

- ⚡ Vite for fast development
- ⚛️ React 19
- 🎨 Tailwind CSS 4
- 📝 TypeScript
- 🧪 Vitest for testing
- 🎯 ESLint for code quality
- 🚀 Ready for deployment

## Contributing

Feel free to open issues or pull requests!